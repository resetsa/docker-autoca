{
    "signing": {
        "profiles": {
            "ocsp": { "auth_remote": { "auth_key": "hidden-key", "remote": "server" }},
            "client": { "auth_remote": { "auth_key": "hidden-key", "remote": "server"}},
            "custom": { "auth_remote": { "auth_key": "hidden-key", "remote": "server" }}
            }
        },
    "auth_keys": {
        "hidden-key": {
            "type": "standard",
            "key": "file:/data/auth.key"
        }
    },
    "remotes": {
        "server": "https://autoca.home.lan:8888"
    }
}
